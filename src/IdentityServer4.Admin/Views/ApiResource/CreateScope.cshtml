@model IdentityServer4.Admin.ViewModels.ApiResource.ApiResourceScopeViewModel

@section breadcrumb{
    <li >
        <a href="/api-resource">API Resource</a>
    </li>
    <li >
        <a href="#">API Resource Scope</a>
    </li>
}

@section header{
    Add API Resource Scope<small v-text="moduleDescription"></small>
}

<div class="col-xs-12">
    <div class="box">
        <form action="@Context.Request.Path" method="post">
            @await Html.PartialAsync("_ValidationSummary")
            <div class="box-body">
                @await Html.PartialAsync("ScopeItem", Model)
            </div>
            <!-- /.box-body -->
            <div class="box-footer clearfix">
                <button type="submit" class="btn btn-default btn-sm  btn-flat">Submit</button>
            </div>
        </form>
    </div>
    <!-- /.box -->
</div>

@section scripts{
    <script>
        $(function() {
            app.activeMenu('ApiResource');
        })
    </script>
}